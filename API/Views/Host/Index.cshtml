@model IEnumerable<Models.Host>

@{
    ViewBag.Title = "Hosts";
    Layout = "_Layout";
}

<div class="container" ng-controller="hostListCtrl">
    <div class="page-header">
        <h3>List of Hosts</h3>
        
        <div class="clearfix">
            <button class="btn btn-default pull-right" ng-click="getAllHostPDF()">Download PDF</button>
        </div>
    </div>
    
    <table class="table table-bordered">
        <thead>
        <tr>
            <th>Fullname</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Address</th>
            <th class="hidden-xs">Action</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var driver in Model)
        {
            <tr>
                <td>@driver.Fullname</td>
                <td>@driver.Email</td>
                <td>@driver.Phone</td>
                <td>@driver.Address</td>
                <td class="hidden-xs">
                    <a href="~/Host/delete/@driver.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>

</div>